"""Vendored, pruned subset of kohya-ss/sd-scripts `library/` for Anima text-to-image.

Only the modules needed for Anima (Cosmos-Predict2 2B text2image) inference are
included, rewritten from `library.` to the `sd_scripts.` namespace, and pruned of
training machinery. See the project README for the vendoring notes.
"""
